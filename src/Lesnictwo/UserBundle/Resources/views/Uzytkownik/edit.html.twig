{% extends '::base.html.twig' %}

{% block body -%}
     <br/>
    <br/>
    <br/>
    <h3 class="col-md-offset-2">Edycja Użytkownika</h3>
    <div class="col-md-4 col-md-offset-4">  
        {{ form_start(edit_form) }}
            {{ form_errors(edit_form) }}

            {{ form_row(edit_form.imie) }}
            {{ form_row(edit_form.nazwisko) }}
            {{ form_row(edit_form.login) }}
            {{ form_row(edit_form.email) }}
            {{ form_row(edit_form.haslo) }}
            {{ form_row(edit_form.lesnictwo) }}
            {{ form_row(edit_form.rodzaj) }}                      
            
            <p></p>
            <div class="row">
                <div class="col-md-3 col-md-offset-2">
                    {{ form_row(edit_form.submit, { 'label': 'Aktualizuj','attr': {'class': 'btn btn-primary'} }) }}
                </div>
                <div class="col-md-3">
                    <a class="btn btn-default" href="{{ path('users') }}">Anuluj</a>
                </div>    
            
            </div>
                <br/>
        {{ form_end(edit_form) }}
        
        
 
{% endblock %}
